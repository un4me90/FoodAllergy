{
  "name": "food-allergy-app",
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend ci --include=dev && npm --prefix frontend run build && npm --prefix backend ci && npm --prefix backend run build",
    "start": "node backend/dist/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
